/**
 * Optional public `t.me` invite for the parish Telegram group.
 * When unset, the UI nudges members to request an invite from the organizer.
 */
export const TELEGRAM_GROUP_INVITE_URL: string | undefined = undefined
